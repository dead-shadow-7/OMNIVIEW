{
  "name": "omniview",
  "version": "1.0.0",
  "description": "",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . omniview --platform=win32 --arch=x64 --icon=src/assets/icon.ico --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "node minify.js && electron-packager . omniview --platform=win32 --arch=x64 --icon=src/assets/icon.ico --overwrite --asar",
    "build:win": "electron-builder --win --x64"
  },
  "build": {
    "appId": "com.example.omniview",
    "win": {
      "icon": "src/assets/icon.ico"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron/packager": "^18.4.0",
    "electron": "^37.2.4",
    "terser": "^5.43.1",
    "electron-builder": "^24.9.1"
  }
}
